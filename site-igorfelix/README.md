# Site Igor Felix Advogados

Site estático gerado com Eleventy. Blog com painel Decap CMS em /admin.

- `src/` conteúdo e modelos (artigos em `src/blog/posts/*.md`)
- `static/` arquivos servidos direto (home, revisão, imagens, /admin, assets)
- Build: `npm run build` (gera `_site/`)
